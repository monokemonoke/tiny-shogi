[package]
name = "minishogi4"
version = "0.1.0"
edition = "2024"

[dependencies]
bincode = "1.3"
serde = { version = "1.0", features = ["derive"] }
tiny_http = "0.12"
serde_json = "1.0"
rusqlite = "0.31"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
opt-level = 3
