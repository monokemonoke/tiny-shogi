{
  "name": "tiny-shogi-mcp",
  "version": "0.1.0",
  "description": "Tiny Shogi analysis MCP server backed by the local lookup.db.",
  "type": "module",
  "bin": {
    "tiny-shogi-mcp": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "better-sqlite3": "^12.10.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^25.0.10",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
